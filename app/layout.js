export const metadata = {
  title: 'Key People Insights',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ backgroundColor: '#112D4E' }} >{children}</body>
    </html>
  )
};
